# Test Reports

Generated by `npm run test:report` (which uses `.report-requested` since Anchor does not forward env vars to the test subprocess). Reports include:

- Transaction signatures
- Compute units consumed
- Solana Explorer links (localnet: `?cluster=custom&customUrl=http://127.0.0.1:8899`)

Example report filename: `test-report-2025-03-08T12-30-45.md`
